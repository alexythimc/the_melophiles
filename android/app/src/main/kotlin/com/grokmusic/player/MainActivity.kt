package com.grokmusic.player

import io.flutter.embedding.android.FlutterFragmentActivity

// Use FlutterFragmentActivity to ensure compatibility with background/audio plugins that expect a
// Fragment-based activity (provides the correct FlutterEngine handling for some plugins).
class MainActivity : FlutterFragmentActivity()
