// song_model_adapter.dart
// Adapter removed: using simple Map storage in Hive 'songs' box instead.
// This file is intentionally left as a stub to avoid build_runner / adapter generation.

// If you later want a binary TypeAdapter for Song, implement it here and register
// it in main() with `Hive.registerAdapter(SongAdapter());`.
